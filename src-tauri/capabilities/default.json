{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the CineVault main window: core runtime permissions (window controls, event listen/emit) plus the dialog plugin used by the manual poster import command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
