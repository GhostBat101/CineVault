{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the CineVault main window: core runtime permissions (window controls, event listen/emit) required by the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
